using UnityEngine;

namespace AF.Dungeon
{
    /// <summary>
    /// Required on every walkable floor slab. Same GameObject must have a MeshFilter.
    /// RoomPrefabData bakes these into FloorTiles for the solver.
    /// </summary>
    public sealed class RoomFloorTile : MonoBehaviour
    {
    }
}
